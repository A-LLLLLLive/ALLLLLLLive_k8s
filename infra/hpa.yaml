apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: alive-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: alive-deployment 
  minReplicas: 12
  maxReplicas: 12
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 30

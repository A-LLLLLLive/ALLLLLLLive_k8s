apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: alive-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: alive-deployment 
  minReplicas: 25
  maxReplicas: 35
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 30


apiVersion: v1
kind: Service
metadata:
  name: {{ include "bechart.fullname" . }}
  labels:
    {{- include "bechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bechart.selectorLabels" . | nindent 4 }}
